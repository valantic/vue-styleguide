/**
 * Root Vue app options merged at startup in src/main.ts.
 *
 * Consumer projects can override this by editing the file — for example to add
 * global `errorHandler`, `warnHandler`, or `compilerOptions`. Keep additions
 * limited to options that apply to the entire app, not individual components.
 *
 * See: https://vuejs.org/api/application#app-config
 */
export default {};
